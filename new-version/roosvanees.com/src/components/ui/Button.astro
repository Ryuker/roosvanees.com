---
import { Icon } from 'astro-icon/components';
import Tooltip from './Tooltip.astro';
const {
  icon, 
  text,
  id,
  tooltipText = '', 
  classes = " ",
  colors = 'bg-pink-200 hover:bg-pink-300',
} = Astro.props;

---

<button id={`btn-${id}`} aria-describedby="tooltip"
  class={`${classes} ${colors} border-2 border-pink-300 px-4 md:px-6 py-3 min-w-20 rounded-lg flex justify-center items-center`}>
  {icon && <Icon name={icon} class="text-lg" /> }
  {text && text}
</button>

{ tooltipText && 
  <Tooltip text={tooltipText} buttonId={`btn-${id}`} />
}